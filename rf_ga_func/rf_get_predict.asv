 function predict_list = rf_get_predict(mdl, data, class_list)
            
    [~, prd_tmp] = predict(mdl, data);

    [max_num, prd] = max(prd_tmp, [], 2);

    predict_list = zeros(data_num, 2);
    sum(max_num == prd_tmp) > 1

    end 
    
 end
